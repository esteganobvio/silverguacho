---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
name: silverguacho-nvidia
# description will be included in the image's metadata
description: SilverGuacho Image
alt-tags:
  - gts
base-image: ghcr.io/blue-build/base-images/fedora-silverblue-nvidia
image-version: gts
# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - from-file: common/common.yml
  - from-file: common/virt-modules.yml
  - from-file: common/os-release.yml
